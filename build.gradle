plugins {
    id 'java-library'
    id 'maven-publish'
}

group 'org.bana'
version '0.0.1-SNAPSHOT'

repositories {
    mavenLocal()
    mavenCentral()
}

dependencies {
    implementation 'org.bana:bana:0.0.1-SNAPSHOT'
    api 'org.mybatis.spring.boot:mybatis-spring-boot-starter:2.2.0'
    api 'com.baomidou:mybatis-plus-boot-starter:3.4.3.1'
    implementation 'com.github.0xshamil:java-xid:1.0.0'
    testImplementation 'org.junit.jupiter:junit-jupiter-api:5.7.2'
    testRuntimeOnly 'org.junit.jupiter:junit-jupiter-engine:5.7.2'
}

test {
    useJUnitPlatform()
}

publishing{
    repositories {
        maven {
            name = 'localRepo'
            url = "file://${buildDir}/repo"
        }
    }
    publications{
        mavenJava(MavenPublication){
            groupId = "org.bana"
            artifactId = "bana-mybatis-adapter"
            version = "0.0.1-SNAPSHOT"

            from components.java
        }
    }
}
